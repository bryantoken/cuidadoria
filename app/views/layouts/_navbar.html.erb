<div class="bg-white sticky top-0 py-4 flex w-full justify-between px-20 drop-shadow-md">
  <div class="gap-8 flex items-center">
  <%= image_tag 'icon', class:'size-10'%>
  <span class="text-black font-extrabold text-xl">Guardiões De Ouro</span>

  <span class="text-black hover:border-b-2 "> <%= link_to 'Home',  welcome_index_path %> </span>
  <span class="text-black hover:border-b-2"> <%= link_to 'Registro Diário',  registro_cuidadors_path %>  </span>
  <span class="text-black hover:border-b-2">Sobre</span>
  </div>


  <div class="flex gap-2 items-center justify-center">
  <% if user_signed_in?%>
    <p class="font-bold text-white">
    <%= current_user.name %>
    </p>
 <%= button_to "Sair", destroy_user_session_path, method: :delete, class: "rounded-lg py-2 px-8 bg-red-600 hover:bg-red-700 text-white block font-bold" %>

  <% else %>
  <%= link_to "Login", user_session_path, class: "rounded-lg py-2 px-8 bg-blue-600 hover:bg-blue-700 text-white block font-bold" %>
  <% end %>


  </div>

</div>
